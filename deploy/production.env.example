# Deployment values, not Vite configuration. Use registry image@sha256:... references.
MESTHI_FRONTEND_IMAGE=
OAUTH2_PROXY_IMAGE=
REDIS_IMAGE=
# Docker network already attached to the existing HTTPS nginx.
MESTHI_EDGE_NETWORK=
