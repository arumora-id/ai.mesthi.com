
import { useState } from 'react'
import { useWorkspace,mode } from '../core/runtime'
import { CreateRun } from './CreateDialogs'
import { downloadText,Field,Icon,PageHeading } from '../components/ui'
const initialScenes=[
{title:'The hook',duration:'0–5s',shot:'A close-up of a crowded desk. A creator faces a long list of things to do.',narration:'What if your next big idea had a team behind it?',color:'sage'},
{title:'The possibility',duration:'5–20s',shot:'Pull back to reveal a shared creative office. Specialists bring an idea to life, together.',narration:'Meet your AI workforce. From the first spark to the final draft.',color:'sand'},
{title:'The invitation',duration:'20–30s',shot:'The creator reviews the finished work. A quiet, confident MESTHI title card.',narration:'A little space. Big possibilities. Make room for your next idea.',color:'blue'}
]
export function ContentStudio(){
 const {workflows,navigate}=useWorkspace();const [brief,setBrief]=useState('Introduce MESTHI to independent creators. A 30-second story about making room for great work. Thoughtful, clear, and optimistic.'),[title,setTitle]=useState('A little space. Big possibilities.'),[platform,setPlatform]=useState('Instagram Reels'),[scenes,setScenes]=useState(initialScenes),[running,setRunning]=useState(false)
 const flow=workflows.find(w=>w.name==='Short-form video')
 const markdown='# '+title+'\n\nDraft storyboard · '+platform+'\n\n## Brief\n'+brief+'\n\n'+scenes.map((s,i)=>'## Scene '+(i+1)+': '+s.title+' ('+s.duration+')\nVisual: '+s.shot+'\nVoiceover: '+s.narration).join('\n\n')+'\n\nStatus: editable sample outline. No video has been rendered or published.'
 return <><PageHeading eyebrow="FROM A SPARK TO A STORY" title="Content studio" text="Shape the idea, frame the story, and give every piece of content a clear direction." action={<button className="button secondary" onClick={()=>downloadText('mesthi-storyboard.md',markdown)}><Icon name="Download" size={16}/>Export draft</button>}/><div className="content-layout"><section className="panel creative-brief"><div className="panel-heading"><h2>The creative brief</h2><Icon name="WandSparkles"/></div><div className="stack gap-5 p-5"><Field label="Working title"><input value={title} onChange={e=>setTitle(e.target.value)} maxLength={140}/></Field><Field label="What story do you want to tell?"><textarea rows={6} value={brief} onChange={e=>setBrief(e.target.value)} maxLength={10000}/></Field><Field label="Destination"><select value={platform} onChange={e=>setPlatform(e.target.value)}><option>Instagram Reels</option><option>TikTok</option><option>X</option><option>Internal draft</option></select></Field><div className="creative-format"><span><Icon name="Film"/>Short video</span><span>9:16</span><span>30 seconds</span></div><button className="button primary" disabled={!flow||!title.trim()||!brief.trim()} onClick={()=>setRunning(true)}><Icon name="Play" size={15}/>{mode==='demo'?'Run demo workflow':'Create content task'}</button>{!flow&&<button className="text-button" onClick={()=>navigate('templates')}>Explore Content Studio pack<Icon name="ArrowRight" size={14}/></button>}<p className="inline-note">This is an editable sample storyboard. Export changes before leaving. Rendering and publishing require connected backend services.</p></div></section><section><div className="storyboard-heading"><div><h2>Your story, frame by frame</h2><p>Three moments. One clear message.</p></div><span className="pill subtle">EDITABLE DRAFT</span></div><div className="storyboard-grid">{scenes.map((s,i)=><article key={i} className="panel scene-card"><div className={'scene-visual '+s.color}><span className="scene-number">{'0'+(i+1)}</span><div className={'scene-art scene-art-'+i}><span/><span/><span/><i/></div><span className="scene-duration">{s.duration}</span></div><div className="scene-body"><span className="eyebrow">{'SCENE 0'+(i+1)}</span><h3>{s.title}</h3><Field label="Visual direction"><textarea rows={5} value={s.shot} onChange={e=>setScenes(scenes.map((s,n)=>n===i?{...s,shot:e.target.value}:s))}/></Field><Field label="Voiceover"><textarea rows={4} value={s.narration} onChange={e=>setScenes(scenes.map((s,n)=>n===i?{...s,narration:e.target.value}:s))}/></Field></div></article>)}</div><div className="content-review-note"><Icon name="ShieldCheck" size={22}/><div><strong>You have the final cut.</strong><p>Review the final media and destination before approving any public post.</p></div><button className="text-button" onClick={()=>navigate('approvals')}>Open approvals<Icon name="ArrowRight" size={15}/></button></div></section></div>{running&&flow&&<CreateRun workflowId={flow.id} initialTitle={title} initialBrief={markdown} onClose={()=>setRunning(false)}/>}</>
}
